/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["scontent-prg1-1.cdninstagram.com","api-gold-e-shop-seven.vercel.app"], // Add the Instagram CDN domain
      },
};

export default nextConfig;
