/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["scontent-prg1-1.cdninstagram.com"], // Add the Instagram CDN domain
      },
};

export default nextConfig;
